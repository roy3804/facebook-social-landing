<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 517 495" width="517" height="495" role="img">
  <title>Phone showing a generic social feed, with two floating tiles on a soft blue field</title>

  <!-- ===========================================================================
       Hero illustration. Vector so it stays sharp past the old 517x495 raster.

       GEOMETRY CONTRACTS the stylesheet depends on. Breaking one is a visible
       seam on the live page, so measure before you commit:
         viewBox       -> exactly "0 0 517 495", width 517, height 495.
         top edge      -> #dbeeff across the FULL width of row y=0. Below 861px
                          the hero stacks and .hero-copy's gradient ends on
                          #dbeeff exactly where the art begins; any other colour
                          at the top reads as a hard line.
         right ~15%    -> every pixel at x >= 439 is bare, uniform #c4e6fc, the
                          colour the desktop copy column's gradient hits at the
                          49% join, where a 72px mask fades the art into it.
                          NOTHING is drawn there. The radials and blobs below
                          are all sized to stop short of it.
         left + bottom -> the field is OPAQUE and runs off both edges. No
                          transparency anywhere, no white margin.
         subject       -> stays inside x 68..430 and below y 72, so object-fit:
                          cover at another aspect ratio never cuts the screen.

       No text elements: system fonts differ per machine, so every label here is
       a drawn placeholder bar the way a UI mockup does it. No lettering of any
       script, anywhere.
       No platform logo, wordmark or product mark of any kind. Every glyph is
       generic and geometric — person, bell, magnifier, heart, speech bubble,
       share arrow. Nothing here is a thumbs-up, an "f", a camera outline, a
       lightning bubble or an infinity loop, and the reaction row uses a HEART
       precisely so it never has to use a thumb.
       Every colour comes from the page's own ramp in app/globals.css: the blue
       scale plus the ink greys. Do not paint anything in a platform brand
       palette: an exact brand quartet in a social-app mockup is trade dress and
       reads as affiliation just as a logo would, which is the one thing the
       footer of this page denies.

       SCREEN — composed for hierarchy, not as an even list:
         header (white)  -> account avatar at the reading edge, search pill,
                            bell button with an unread badge at the far end.
                            Search is folded into the bar rather than given its
                            own band; that is what buys room for the stories.
         stories strip   -> six bubbles running right to left, the ring ramp
                            cooling from blue-500 (#2b7cf0) to near-white and the
                            silhouettes fading out with it. That ramp is the
                            one thing in the picture that is about the OFFER
                            rather than about "social" in general: the recent
                            end is alive, the far end has gone quiet.
         card 1          -> the dominant post: shadowed, white on a blue-50
                            feed ground, byline / copy / image block /
                            reaction row.
         card 2          -> the same anatomy an order quieter — hairline, no
                            shadow, paler bars — cut by the bottom of the
                            canvas so the feed reads as continuing. Everything
                            in it below local y=415 is off-canvas and has been
                            deleted; do not add detail there.
       The accents sit on the RIGHT half of the screen — the bright story ring,
       the post avatar, the filled heart — and the share arrow points right, so
       the illustration hands the eye off toward the Hebrew copy instead of
       holding it. The glass sheen is deliberately weak (peak 0.18, not 0.5):
       at 0.5 it laid a 29% white wash over exactly those accents and undid the
       composition. Raise it again and you lose them.

       RTL: the status bar is mirrored (radios left, time right), avatars sit at
       the right reading edge, placeholder bars are flush right and ragged left,
       overflow dots park at the far end, and each count bar trails to the LEFT
       of its glyph.
       ======================================================================== -->

  <defs>
    <!-- Field ============================================================== -->
    <!-- Lifts the very top to #dbeeff and settles back onto the #c4e6fc base
         by y=88, so the right strip is uniform over 82% of its height. -->
    <linearGradient id="topFade" x1="0" y1="0" x2="0" y2="88" gradientUnits="userSpaceOnUse">
      <stop offset="0" stop-color="#dbeeff"/>
      <stop offset="1" stop-color="#dbeeff" stop-opacity="0"/>
    </linearGradient>

    <!-- The deep end of the field. Both radials are sized so their outer edge
         stops short of x=439 at every height — that is what keeps the right
         strip flat. -->
    <radialGradient id="deepA" cx="-40" cy="520" r="388" gradientUnits="userSpaceOnUse">
      <stop offset="0" stop-color="#5eb3ee"/>
      <stop offset="0.55" stop-color="#8fcdf5" stop-opacity="0.72"/>
      <stop offset="1" stop-color="#c4e6fc" stop-opacity="0"/>
    </radialGradient>
    <radialGradient id="deepB" cx="62" cy="566" r="298" gradientUnits="userSpaceOnUse">
      <stop offset="0" stop-color="#3f9fe4" stop-opacity="0.85"/>
      <stop offset="1" stop-color="#3f9fe4" stop-opacity="0"/>
    </radialGradient>

    <!-- Phone ============================================================== -->
    <!-- userSpaceOnUse resolves in the phone group's own local coordinates. -->
    <linearGradient id="frame" x1="0" y1="0" x2="248" y2="0" gradientUnits="userSpaceOnUse">
      <stop offset="0" stop-color="#63738d"/>
      <stop offset="0.05" stop-color="#2b3547"/>
      <stop offset="0.5" stop-color="#1b2431"/>
      <stop offset="0.95" stop-color="#303c50"/>
      <stop offset="1" stop-color="#6b7b95"/>
    </linearGradient>
    <!-- Weak and broad. See the note above: this used to peak at 0.5 and it
         bleached the very accents the screen is composed around. -->
    <linearGradient id="glass" x1="1" y1="0" x2="0.18" y2="0.52">
      <stop offset="0" stop-color="#ffffff" stop-opacity="0.18"/>
      <stop offset="0.5" stop-color="#ffffff" stop-opacity="0"/>
    </linearGradient>
    <clipPath id="screenClip">
      <rect x="11" y="11" width="226" height="498" rx="32"/>
    </clipPath>

    <!-- Post image block. A soft blue wash with two abstract bands and a light
         disc — enough to read as "a photo", too abstract to depict anything.
         The bands are both anchored to the bottom edge so they stack instead
         of crossing; two crossing translucent shapes render their overlap as a
         hard-edged lens and read as a mistake. -->
    <linearGradient id="photo" x1="0" y1="0" x2="0.85" y2="1">
      <stop offset="0" stop-color="#e3eeff"/>
      <stop offset="0.55" stop-color="#bbd8fb"/>
      <stop offset="1" stop-color="#8dbcf5"/>
    </linearGradient>
    <clipPath id="photoClip">
      <rect x="34" y="240" width="180" height="78" rx="12"/>
    </clipPath>

    <!-- Tiles ============================================================== -->
    <linearGradient id="tileBlue" x1="0" y1="0" x2="0.85" y2="1">
      <stop offset="0" stop-color="#5e9df9"/>
      <stop offset="1" stop-color="#3a78ee"/>
    </linearGradient>
    <linearGradient id="tileWhite" x1="0" y1="0" x2="0.6" y2="1">
      <stop offset="0" stop-color="#ffffff"/>
      <stop offset="1" stop-color="#edf5ff"/>
    </linearGradient>

    <!-- Shadows ============================================================ -->
    <!-- Offset left and down: the spill lands on the field it bleeds into,
         never toward the reserved right strip. -->
    <filter id="phoneShadow" x="-25%" y="-12%" width="150%" height="126%">
      <feGaussianBlur stdDeviation="14"/>
    </filter>
    <filter id="tileShadow" x="-35%" y="-28%" width="170%" height="176%">
      <feDropShadow dx="0" dy="7" stdDeviation="5" flood-color="#0b3f7d" flood-opacity="0.22"/>
    </filter>
    <!-- Lifts the dominant post card off the feed ground. The second card gets
         a hairline only — that difference is the whole hierarchy. -->
    <filter id="cardShadow" x="-14%" y="-12%" width="128%" height="128%">
      <feDropShadow dx="0" dy="3" stdDeviation="4.5" flood-color="#0b3f7d" flood-opacity="0.11"/>
    </filter>
  </defs>

  <!-- === 1. Field ========================================================= -->
  <rect x="0" y="0" width="517" height="495" fill="#c4e6fc"/>
  <rect x="0" y="0" width="517" height="495" fill="url(#deepA)"/>
  <rect x="0" y="0" width="517" height="495" fill="url(#deepB)"/>

  <!-- === 2. Soft blobs behind everything ================================== -->
  <!-- All of these stay left of x=425; the right strip never sees them. -->
  <circle cx="250" cy="118" r="132" fill="#ffffff" fill-opacity="0.34"/>
  <circle cx="110" cy="120" r="95" fill="none" stroke="#ffffff" stroke-opacity="0.36" stroke-width="16"/>
  <circle cx="30" cy="248" r="106" fill="#9ed3f6" fill-opacity="0.55"/>
  <circle cx="-12" cy="432" r="120" fill="#6cbdf2" fill-opacity="0.5"/>
  <circle cx="80" cy="430" r="140" fill="none" stroke="#ffffff" stroke-opacity="0.28" stroke-width="22"/>
  <circle cx="152" cy="472" r="74" fill="#8fcdf5" fill-opacity="0.34"/>

  <!-- === 3. Top wash ====================================================== -->
  <!-- Drawn over the blobs so the first pixel row is exactly #dbeeff no matter
       what passes underneath. -->
  <rect x="0" y="0" width="517" height="88" fill="url(#topFade)"/>

  <!-- === 4. Phone ========================================================= -->
  <!-- Body 248x520 drawn from its own origin, then tilted -6deg about its
       centre at (226,355). Corners land at roughly (68,109) top-left and
       (322,84) top-right, and it runs off the bottom edge. The tilt rises to
       the right, which is the same direction the composition wants the eye to
       travel, so the feed inside is laid out to agree with it. -->
  <g transform="translate(226 355) rotate(-6) translate(-124 -260)">

    <rect x="-6" y="10" width="248" height="520" rx="42" fill="#12447f" fill-opacity="0.26" filter="url(#phoneShadow)"/>

    <!-- Side hardware -->
    <rect x="-3" y="150" width="4" height="22" rx="2" fill="#3c4a60"/>
    <rect x="-3" y="184" width="4" height="38" rx="2" fill="#3c4a60"/>
    <rect x="-3" y="232" width="4" height="38" rx="2" fill="#3c4a60"/>
    <rect x="247" y="198" width="4" height="54" rx="2" fill="#3c4a60"/>

    <!-- Chassis -->
    <rect x="0" y="0" width="248" height="520" rx="42" fill="url(#frame)"/>
    <rect x="5" y="5" width="238" height="510" rx="37" fill="#0d131c"/>
    <rect x="11" y="11" width="226" height="498" rx="32" fill="#ffffff"/>

    <g clip-path="url(#screenClip)">

      <!-- Status bar, MIRRORED for a Hebrew locale: radios at the left edge,
           time block at the right, on matching 11-unit insets. The ORDER of
           the cluster mirrors (battery outermost) but the glyphs themselves do
           not — the battery still fills from its flat end and the signal bars
           still climb left to right, the way a platform's own RTL status bar
           leaves non-directional symbols alone. Mirroring the bars too made
           them read as signal dropping away, which is a note this picture does
           not want to sound. -->
      <rect x="22" y="26.5" width="20" height="10" rx="3" fill="none" stroke="#5b6b80" stroke-width="1.6"/>
      <rect x="24" y="28.5" width="13" height="6" rx="1.6" fill="#48586e"/>
      <rect x="43.5" y="29.5" width="2.4" height="4" rx="1.2" fill="#5b6b80"/>
      <path d="M49 29.5a13 13 0 0 1 17 0" fill="none" stroke="#48586e" stroke-width="2.2" stroke-linecap="round"/>
      <path d="M52.5 33.5a8 8 0 0 1 10 0" fill="none" stroke="#48586e" stroke-width="2.2" stroke-linecap="round"/>
      <circle cx="57.5" cy="36" r="1.8" fill="#48586e"/>
      <rect x="70.4" y="31" width="3.6" height="5" rx="1.6" fill="#48586e"/>
      <rect x="76.4" y="29" width="3.6" height="7" rx="1.6" fill="#48586e"/>
      <rect x="82.4" y="27" width="3.6" height="9" rx="1.6" fill="#48586e"/>
      <rect x="88.4" y="25" width="3.6" height="11" rx="1.6" fill="#48586e"/>
      <rect x="196" y="27" width="30" height="9" rx="4.5" fill="#48586e"/>

      <!-- Camera cutout. A narrow pill punch-hole with the lens offset inside
           it — deliberately NOT the wide centred housing with an offset lens,
           which is one specific vendor's silhouette rather than a neutral
           phone's. -->
      <rect x="102" y="20" width="44" height="15" rx="7.5" fill="#0d131c"/>
      <circle cx="136" cy="27.5" r="3.3" fill="#1c2a3d"/>

      <!-- Header ========================================================= -->
      <!-- Reads right-to-left like the page: the account avatar sits at the
           reading edge, the search pill runs across, and a single round button
           closes the row at the far end. The old build spent a whole 48px band
           on a standalone search field under the bar; folding the search into
           the bar buys that band back for the stories strip, which is what
           makes the screen read as social rather than as a list. -->

      <!-- Account avatar at the reading edge. Tinted disc with a blue-700
           silhouette (4.5:1 inside itself): this is the row's anchor, so it
           has to be the crispest thing in it, not the palest. A white
           silhouette on a pale disc measured 1.35:1 and read as a smudge. -->
      <circle cx="213" cy="74" r="13" fill="#a9ccf8"/>
      <circle cx="213" cy="70.3" r="4.36" fill="#0b4fba"/>
      <path d="M205.53 84.63a7.48 8.58 0 0 1 14.95 0A13 13 0 0 1 205.53 84.63Z" fill="#0b4fba"/>

      <!-- Search pill: magnifier at the reading edge, placeholder bar trailing -->
      <rect x="60" y="62" width="130" height="24" rx="12" fill="#f2f7fd" stroke="#e4edf8" stroke-width="1.4"/>
      <circle cx="178" cy="72" r="5" fill="none" stroke="#7b93b1" stroke-width="2.2"/>
      <path d="M174.4 75.8 170.9 79.3" stroke="#7b93b1" stroke-width="2.2" stroke-linecap="round"/>
      <rect x="112" y="71" width="52" height="6" rx="3" fill="#c6d5e6"/>

      <!-- Bell button with an unread badge -->
      <circle cx="36" cy="74" r="15" fill="#eaf2fc"/>
      <g transform="translate(36 74) scale(0.8) translate(-70 -70.9)" fill="#7b93b1">
        <circle cx="70" cy="60.6" r="1.7"/>
        <path d="M59.5 78 L62 74 V69.5 A8 8 0 0 1 78 69.5 V74 L80.5 78 Z"/>
        <path d="M66.7 79.6a3.3 3.3 0 0 0 6.6 0z"/>
      </g>
      <circle cx="46" cy="64.5" r="5.6" fill="#ffffff"/>
      <circle cx="46" cy="64.5" r="4" fill="#2b7cf0"/>

      <!-- Feed ground ==================================================== -->
      <!-- The header stays paper-white and the feed sits on blue-50, so the
           post cards can be plain white and still separate. -->
      <rect x="0" y="98" width="248" height="430" fill="#f4f9ff"/>
      <rect x="0" y="96.8" width="248" height="1.2" fill="#dce6f1"/>

      <!-- Stories strip ================================================== -->
      <!-- Six bubbles running right to left, the left-most cut by the screen
           edge so the row reads as scrollable. The ring ramp runs bright to
           pale from RIGHT to LEFT and the silhouettes fade with it: the
           strongest accent on the screen is the one nearest the copy column,
           and the far end has gone blank. Every avatar on this screen is built
           from the same two shapes at the same ratios — head r = 0.335R at
           -0.285R, torso an elliptical dome whose corners land exactly on the
           disc edge — so head and shoulders never fuse and no two silhouettes
           are different animals. -->

      <circle cx="211" cy="125" r="19" fill="none" stroke="#2b7cf0" stroke-width="2.6"/>
      <circle cx="211" cy="125" r="15" fill="#6aa5f2"/>
      <circle cx="211" cy="120.7" r="5.03" fill="#ffffff"/>
      <path d="M202.375 137.27a8.63 9.9 0 0 1 17.25 0A15 15 0 0 1 202.375 137.27Z" fill="#ffffff"/>
      <rect x="200" y="148" width="22" height="4" rx="2" fill="#c6d5e6"/>

      <circle cx="167" cy="125" r="19" fill="none" stroke="#6aa5f2" stroke-width="2.6"/>
      <circle cx="167" cy="125" r="15" fill="#a9ccf8"/>
      <circle cx="167" cy="120.7" r="5.03" fill="#ffffff"/>
      <path d="M158.375 137.27a8.63 9.9 0 0 1 17.25 0A15 15 0 0 1 158.375 137.27Z" fill="#ffffff"/>
      <rect x="156" y="148" width="22" height="4" rx="2" fill="#cbd9ea"/>

      <circle cx="123" cy="125" r="19" fill="none" stroke="#a9ccf8" stroke-width="2.6"/>
      <circle cx="123" cy="125" r="15" fill="#c6d5e6"/>
      <circle cx="123" cy="120.7" r="5.03" fill="#ffffff"/>
      <path d="M114.375 137.27a8.63 9.9 0 0 1 17.25 0A15 15 0 0 1 114.375 137.27Z" fill="#ffffff"/>
      <rect x="112" y="148" width="22" height="4" rx="2" fill="#dce6f1"/>

      <circle cx="79" cy="125" r="19" fill="none" stroke="#cbd9ea" stroke-width="2.6"/>
      <circle cx="79" cy="125" r="15" fill="#dce6f1"/>
      <circle cx="79" cy="120.7" r="5.03" fill="#ffffff" fill-opacity="0.8"/>
      <path d="M70.375 137.27a8.63 9.9 0 0 1 17.25 0A15 15 0 0 1 70.375 137.27Z" fill="#ffffff" fill-opacity="0.8"/>
      <rect x="68" y="148" width="22" height="4" rx="2" fill="#e4edf8"/>

      <circle cx="35" cy="125" r="19" fill="none" stroke="#dce6f1" stroke-width="2.6"/>
      <circle cx="35" cy="125" r="15" fill="#e4edf8"/>
      <circle cx="35" cy="120.7" r="5.03" fill="#ffffff" fill-opacity="0.55"/>
      <path d="M26.375 137.27a8.63 9.9 0 0 1 17.25 0A15 15 0 0 1 26.375 137.27Z" fill="#ffffff" fill-opacity="0.55"/>
      <rect x="24" y="148" width="22" height="4" rx="2" fill="#eaf1f9"/>

      <!-- Sixth bubble, mostly past the screen edge -->
      <circle cx="-9" cy="125" r="19" fill="none" stroke="#e4edf8" stroke-width="2.6"/>
      <circle cx="-9" cy="125" r="15" fill="#eaf1f9"/>

      <!-- Card 1, the dominant post ====================================== -->
      <!-- Shadowed, full padding, four-part body: byline, copy, image block,
           reaction row. Everything that carries colour is on its right half. -->
      <rect x="18" y="158" width="212" height="200" rx="18" fill="#ffffff" filter="url(#cardShadow)"/>

      <!-- Byline: avatar at the reading edge, name and meta trailing away from
           it, the overflow dots parked at the far end. -->
      <circle cx="199" cy="184" r="14.5" fill="#2b7cf0"/>
      <circle cx="199" cy="179.9" r="4.86" fill="#ffffff"/>
      <path d="M190.66 195.86a8.34 9.57 0 0 1 16.68 0A14.5 14.5 0 0 1 190.66 195.86Z" fill="#ffffff"/>
      <rect x="114" y="176.5" width="60" height="9" rx="4.5" fill="#7b93b1"/>
      <rect x="136" y="191" width="38" height="6" rx="3" fill="#cbd9ea"/>
      <circle cx="38" cy="184" r="2.4" fill="#c6d5e6"/>
      <circle cx="46" cy="184" r="2.4" fill="#c6d5e6"/>
      <circle cx="54" cy="184" r="2.4" fill="#c6d5e6"/>

      <!-- Copy: two placeholder lines, both flush to the reading edge at
           x=214 and ragged on the left, long line first then short — the way
           a real two-line paragraph sets. -->
      <rect x="34" y="208" width="180" height="8" rx="4" fill="#9db3cd"/>
      <rect x="68" y="222" width="146" height="8" rx="4" fill="#b9c8da"/>

      <!-- Image block -->
      <g clip-path="url(#photoClip)">
        <rect x="34" y="240" width="180" height="78" fill="url(#photo)"/>
        <circle cx="172" cy="262" r="13.5" fill="#ffffff" fill-opacity="0.5"/>
        <path d="M34 318V300c26-13 44 6 68 0s40 10 112 0v18z" fill="#ffffff" fill-opacity="0.3"/>
        <path d="M34 318v-8c30-10 62 4 98-4s56 4 82-2v14z" fill="#2b7cf0" fill-opacity="0.16"/>
      </g>
      <rect x="34" y="327" width="180" height="1" fill="#e4edf8"/>

      <!-- Reaction row. Heart, speech bubble, share arrow — all generic, and
           deliberately NO thumbs-up. Three even items reading right to left,
           each a glyph with its count bar trailing to its LEFT, counts
           descending (likes > comments > shares). The share arrow used to sit
           alone at the far end with no count and a quarter-card of empty space
           in front of it; it now belongs to the row's rhythm.
           Glyph strokes are #7b93b1 = 3.2:1 on white, so the row survives the
           drop to 390px where it is the element that says "social". -->

      <path transform="translate(205 338.4) scale(1.12)" d="M0 6.8C-1.9 4.9-7.8 1.6-7.8-2.4c0-3.2 3.6-4.8 6.2-2.7.8.7 1.3 1.5 1.6 2.1.3-.6.8-1.4 1.6-2.1 2.6-2.1 6.2-.5 6.2 2.7C7.8 1.6 1.9 4.9 0 6.8z" fill="#0962ed"/>
      <rect x="167" y="336" width="23" height="6" rx="3" fill="#a9ccf8"/>

      <!-- Bubble body is symmetric about the group origin and the tail leaves
           the bottom edge on the corner tangent, so the outline closes without
           a spur. Offset +1.75 in y because the tail drags the shape's optical
           centre down; that lands the body's centre on the count bars. -->
      <g transform="translate(147 340.75)" fill="none" stroke="#7b93b1" stroke-width="2.2" stroke-linejoin="round" stroke-linecap="round">
        <path d="M-4.5 -7 H4.5 A3.5 3.5 0 0 1 8 -3.5 V0 A3.5 3.5 0 0 1 4.5 3.5 H-1.5 L-5.5 7.6 L-4.5 3.5 A3.5 3.5 0 0 1 -8 0 V-3.5 A3.5 3.5 0 0 1 -4.5 -7 Z"/>
      </g>
      <rect x="109" y="336" width="23" height="6" rx="3" fill="#cbd9ea"/>

      <!-- Share arrow: stroked stem into a SOLID head. As two stroked chevron
           arms the lower arm ran nearly parallel to the stem and the acute
           wedge between them filled in at render size, turning the arrow into
           a hook. A filled head cannot close up, and the stem tucks inside it
           so there is no join to blob. -->
      <g transform="translate(88.5 339)">
        <path d="M-7 6 C-7 -2.6 -1.6 -6.2 4.2 -6.2" fill="none" stroke="#7b93b1" stroke-width="2.2" stroke-linecap="round"/>
        <path d="M3.4 -9.6 8.6 -6.2 3.4 -2.8 Z" fill="#7b93b1"/>
      </g>
      <rect x="58" y="336.5" width="16" height="5.5" rx="2.75" fill="#dce6f1"/>

      <!-- Card 2, the quiet one, cut by the bottom of the canvas ========= -->
      <!-- Same anatomy, one order down in weight: no shadow, a hairline that
           can actually be seen (#c6d5e6 is 1.4:1 on the feed ground; the old
           #e9f0fa was 1.03:1 and did nothing), paler avatar and bars.
           The canvas edge crosses the screen at local y 389 (left) to 413
           (right), so only the top edge, the avatar and the byline are ever
           visible — everything that used to sit below that has been removed. -->
      <rect x="18" y="368" width="212" height="150" rx="18" fill="#ffffff" stroke="#c6d5e6" stroke-width="1.2"/>
      <circle cx="199" cy="394" r="13" fill="#a9ccf8"/>
      <circle cx="199" cy="390.3" r="4.36" fill="#ffffff"/>
      <path d="M191.53 404.63a7.48 8.58 0 0 1 14.95 0A13 13 0 0 1 191.53 404.63Z" fill="#ffffff"/>
      <rect x="122" y="387" width="54" height="8" rx="4" fill="#b9c8da"/>
      <rect x="142" y="400" width="34" height="5.5" rx="2.75" fill="#dce6f1"/>
      <circle cx="38" cy="394" r="2.2" fill="#dce6f1"/>
      <circle cx="46" cy="394" r="2.2" fill="#dce6f1"/>
      <circle cx="54" cy="394" r="2.2" fill="#dce6f1"/>

      <!-- Glass sheen, last so it sits over the UI -->
      <rect x="11" y="11" width="226" height="498" rx="32" fill="url(#glass)"/>
    </g>
  </g>

  <!-- === 5. Floating tiles ================================================ -->
  <!-- Descending sizes up the right of the phone.
       Right-most extent is 419.5; the drop shadows fade out well before 439.
       Each one laps a little over the phone's right edge, which is what puts
       them in front of it rather than beside it.

       BOTH glyphs are the literal paths from components/Icons.tsx — FeatureIcon
       "account" and FeatureIcon "chat", same coordinates, same 48 grid, same
       stroke ratio — so the hero and the feature-card icon row downpage are
       provably one vocabulary rather than merely a similar idea. They are
       outline icons here and the in-screen avatars are filled silhouettes;
       that is the intended distinction between an app icon and a person.

       The PERSON takes the big blue tile and the BUBBLE takes the small white
       one, and that assignment is not arbitrary: a white speech bubble
       centred on a blue-gradient rounded square is the retired Messenger app
       icon almost exactly, and "almost exactly" is the whole problem on a page
       that runs a paid Meta campaign and denies any Meta connection in its own
       footer. Inverted onto a white tile the same bubble reads as a plain chat
       glyph and resembles nothing. It also puts the better glyph forward:
       accounts are what this page is about, and the bubble is the smaller,
       secondary call to WhatsApp. Do not swap these back. -->

  <!-- Tile 1 — person (FeatureIcon "account", verbatim) -->
  <g transform="translate(366 134) rotate(-4)">
    <rect x="-50" y="-50" width="100" height="100" rx="28" fill="url(#tileBlue)" filter="url(#tileShadow)"/>
    <rect x="-48.5" y="-48.5" width="97" height="97" rx="26.5" fill="none" stroke="#ffffff" stroke-opacity="0.28" stroke-width="1.6"/>
    <g transform="scale(1.5) translate(-24 -25)" fill="none" stroke="#ffffff" stroke-width="3.2" stroke-linecap="round" stroke-linejoin="round">
      <circle cx="24" cy="16" r="8"/>
      <path d="M9 42v-5c0-9 30-9 30 0v5"/>
    </g>
  </g>

  <!-- Tile 2 — speech bubble (FeatureIcon "chat", verbatim) -->
  <g transform="translate(374 266) rotate(5)">
    <rect x="-42" y="-42" width="84" height="84" rx="24" fill="url(#tileWhite)" filter="url(#tileShadow)"/>
    <rect x="-41" y="-41" width="82" height="82" rx="23" fill="none" stroke="#e3eefb" stroke-width="1.5"/>
    <g transform="scale(1.22) translate(-23 -24)" fill="none" stroke="#0b4fba" stroke-width="3.3" stroke-linecap="round" stroke-linejoin="round">
      <path d="M40 22c0 9-8 16-18 16l-13 4 3-10c-4-3-6-6-6-10C6 13 14 6 23 6s17 7 17 16Z"/>
      <path d="M15 22h.1m8 0h.1m8 0h.1"/>
    </g>
  </g>

</svg>
